const path = require("path");

module.exports = {
  config: path.resolve("config", "config.js"), // Sesuaikan dengan lokasi file konfigurasi database Anda
  "models-path": path.resolve("models"), // Sesuaikan dengan lokasi model Sequelize Anda
  "seeders-path": path.resolve("seeders"), // Sesuaikan dengan lokasi seeders Sequelize Anda
  "migrations-path": path.resolve("migrations"), // Sesuaikan dengan lokasi migrations Sequelize Anda
};

// sequelize migration configuration -> create file: type nul > .sequelizerc
// .sequelizerc ini akan digunakan oleh Sequelize ketika Anda menjalankan perintah-perintah seperti sequelize init, sequelize model:generate, dan sebagainya. File ini memungkinkan Anda mengonfigurasi lokasi file konfigurasi, model, seeders, dan migrations yang digunakan oleh Sequelize dalam proyek Node.js Anda.
